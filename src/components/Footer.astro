---
/**
 * Footer.astro
 * Site footer with letterbox effect and expandable accordion for colophon/licensing/privacy
 */

const currentYear = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="site-footer__top-image"></div>
  
  <div class="site-footer__content">
    <!-- Left 1/3: Static content -->
    <div class="site-footer__static">
      <div class="site-footer__brand">
        <span class="site-footer__logo">maexmaya</span>
        <p class="site-footer__tagline">from the margins. outside the algorithm.</p>
      </div>

      <div class="site-footer__connect">
        <p class="site-footer__text">not on x. not on meta.<br />only at <a href="https://bsky.app/profile/maexmaya.com" target="_blank" rel="noopener noreferrer">bluesky</a> and <a href="https://www.patreon.com/maexmaya" target="_blank" rel="noopener noreferrer">patreon</a>.</p>
      </div>

      <div class="site-footer__meta">
        <span class="site-footer__copyright">© {currentYear} maexmaya</span>
      </div>
    </div>

    <!-- Right 2/3: Accordion -->
    <div class="footer-accordion" data-footer-accordion>
      <!-- Colophon -->
      <article class="footer-accordion__card" data-accordion-card>
        <div class="footer-accordion__tab" data-accordion-tab>
          <button class="footer-accordion__toggle" data-toggle aria-expanded="false" aria-label="Expand colophon">
            <span class="footer-accordion__icon">+</span>
          </button>
          <span class="footer-accordion__tab-title">Colophon</span>
        </div>
        <div class="footer-accordion__content">
          <div class="footer-accordion__inner">
            <h2 class="footer-accordion__title">Colophon</h2>
            <div class="footer-accordion__body">
              <p>This site is built with Astro and Tailwind—tools that let me make something that actually looks and feels like mine instead of another cookie-cutter template with the same hamburger menu and interchangeable sections as every other website on the internet.</p>
              <p>I miss when having a website meant spending hours making something weird and personal in Paint Shop Pro, hand-coding the HTML, and putting it out there for whoever wandered by. I built this because I refuse to participate in platforms that amplify fascists and nazis, and I'd rather bypass traditional publishing entirely than be required to post on X or Instagram in the name of marketing and building a brand. I write what I want to write, and I say what I want to say, where I want to say it.</p>
              <p>Fascists, racists, transphobes, and billionaires aren't welcome here.</p>
            </div>
          </div>
        </div>
      </article>

      <!-- Licensing -->
      <article class="footer-accordion__card" data-accordion-card>
        <div class="footer-accordion__tab" data-accordion-tab>
          <button class="footer-accordion__toggle" data-toggle aria-expanded="false" aria-label="Expand licensing">
            <span class="footer-accordion__icon">+</span>
          </button>
          <span class="footer-accordion__tab-title">Licensing</span>
        </div>
        <div class="footer-accordion__content">
          <div class="footer-accordion__inner">
            <h2 class="footer-accordion__title">Licensing</h2>
            <div class="footer-accordion__body">
              <p>The stories on this site are licensed under CC BY-NC-SA—you're free to remix, adapt, or create transformative works as long as you credit me, link back, don't profit without permission, and share your work under the same terms.</p>
            </div>
          </div>
        </div>
      </article>

      <!-- Privacy -->
      <article class="footer-accordion__card" data-accordion-card>
        <div class="footer-accordion__tab" data-accordion-tab>
          <button class="footer-accordion__toggle" data-toggle aria-expanded="false" aria-label="Expand privacy">
            <span class="footer-accordion__icon">+</span>
          </button>
          <span class="footer-accordion__tab-title">Privacy</span>
        </div>
        <div class="footer-accordion__content">
          <div class="footer-accordion__inner">
            <h2 class="footer-accordion__title">Privacy</h2>
            <div class="footer-accordion__body">
              <p>Most websites have privacy policies because they need to disclose all the ways they're tracking you. This one exists to tell you I'm not.</p>
              <p>I built this site without analytics, cookies, or tracking pixels, which means I have no idea how many people visit, what they read, or how long they stay. The internet wasn't supposed to be a surveillance machine, and I wanted to make something that works the way websites used to—you show up, you read what you want, and you leave without a trace.</p>
              <p>If you want me to know you were here, you'll have to tell me yourself. Find me on Bluesky or support the work on Patreon. Otherwise, enjoy the anonymity.</p>
            </div>
          </div>
        </div>
      </article>
    </div>
  </div>

  <div class="site-footer__bottom-image"></div>
</footer>

<style>
  .site-footer {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    position: relative;
  }

  /* Top image band - 15% height */
  .site-footer__top-image {
    height: 15vh;
    background-image: url('/images/bg.png');
    background-size: cover;
    background-position: top center;
    position: relative;
  }

  .site-footer__top-image::after {
    content: '';
    position: absolute;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.7);
  }

  /* Middle content band - 70% height, 3-column grid */
.site-footer__content {
  height: 70vh;
  max-height: 70vh; /* Add this */
  background-color: var(--color-ink);
  display: grid;
  grid-template-columns: 1fr 2fr;
  padding-left: var(--space-lg);
  overflow: hidden; /* Add this */
}

  /* Left static content */
  .site-footer__static {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: var(--space-lg);
    padding-right: var(--space-lg);
  }

  .site-footer__brand {
    /* no max-width needed */
  }

  .site-footer__logo {
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 700;
    display: block;
    margin-bottom: var(--space-sm);
    color: var(--color-paper);
  }

  .site-footer__tagline {
    font-size: 0.9375rem;
    color: rgba(250, 248, 245, 0.6);
    line-height: 1.6;
  }

  .site-footer__connect {
    /* no max-width needed */
  }

  .site-footer__text {
    font-size: 0.9375rem;
    color: rgba(250, 248, 245, 0.6);
    line-height: 1.6;
  }

  .site-footer__text a {
    color: var(--color-accent);
    transition: color var(--duration-fast) var(--ease-out-quart);
  }

  .site-footer__text a:hover {
    color: var(--color-paper);
  }

  .site-footer__meta {
    font-size: 0.75rem;
    color: rgba(250, 248, 245, 0.4);
  }

  /* Accordion - right 2/3 */
.footer-accordion {
  display: flex;
  flex-direction: row;
  height: 100%;
  max-height: 100%;
  justify-content: flex-end;
  overflow: hidden;
  }

.footer-accordion__card {
  display: flex;
  flex-direction: row;
  height: 100%;
  max-height: 100%;
  border-left: 1px solid rgba(250, 248, 245, 0.1);
  transition: flex 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  flex: 0 0 50px;
  min-width: 0;
  overflow: hidden;
  }

  .footer-accordion__card.is-expanded {
    flex: 1 1 300px;
  }

  /* Tab - vertical strip */
  .footer-accordion__tab {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1rem 0.5rem;
    gap: 1rem;
    width: 50px;
    height: 100%;
    flex-shrink: 0;
    justify-content: flex-start;
    background-color: var(--color-ink);
  }

  .footer-accordion__toggle {
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    font-weight: 300;
    color: var(--color-paper);
    background: none;
    border: 1px solid rgba(250, 248, 245, 0.3);
    cursor: pointer;
    transition: all 0.2s ease;
    line-height: 1;
    flex-shrink: 0;
  }

  .footer-accordion__toggle:hover {
    border-color: var(--color-paper);
  }

  .footer-accordion__card.is-expanded .footer-accordion__toggle {
    transform: rotate(45deg);
  }

  .footer-accordion__tab-title {
    writing-mode: vertical-rl;
    text-orientation: mixed;
    font-family: var(--font-body);
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: rgba(250, 248, 245, 0.6);
    white-space: nowrap;
  }

  /* Content area */
  .footer-accordion__content {
    flex: 1;
    height: 100%;
    overflow: hidden;
    opacity: 0;
    width: 0;
    transition: opacity 0.3s ease;
  }

  .footer-accordion__card.is-expanded .footer-accordion__content {
    opacity: 1;
    width: auto;
    overflow-y: auto;
  }

  .footer-accordion__inner {
    padding: 2rem;
    max-width: 500px;
  }

  .footer-accordion__title {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-paper);
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: -0.02em;
  }

  .footer-accordion__body {
    font-size: 0.875rem;
    line-height: 1.7;
    color: rgba(250, 248, 245, 0.7);
  }

  .footer-accordion__body p {
    margin: 0 0 1rem 0;
  }

  .footer-accordion__body p:last-child {
    margin-bottom: 0;
  }

  /* Bottom image band - 15% height */
  .site-footer__bottom-image {
    height: 15vh;
    background-image: url('/images/bg.png');
    background-size: cover;
    background-position: bottom center;
    position: relative;
  }

  .site-footer__bottom-image::after {
    content: '';
    position: absolute;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.7);
  }

  /* Mobile */
@media (max-width: 768px) {
  .site-footer__content {
    grid-template-columns: 1fr;
    height: auto;
    max-height: none;
    min-height: 70vh;
    padding: var(--space-lg);
    overflow: visible;
  }

  .site-footer__static {
    padding-right: 0;
    padding-bottom: var(--space-xl);
  }

  .footer-accordion {
    flex-direction: column;
    height: auto;
    max-height: none;
    overflow: visible;
  }

  .footer-accordion__card {
    flex-direction: column;
    border-left: none;
    border-top: 1px solid rgba(250, 248, 245, 0.1);
    flex: none;
    height: auto;
    max-height: none;
    overflow: visible;
  }

  .footer-accordion__card.is-expanded {
    flex: none;
  }

  .footer-accordion__tab {
    flex-direction: row;
    width: 100%;
    height: auto;
    padding: 0.75rem 1rem;
  }

  .footer-accordion__tab-title {
    writing-mode: horizontal-tb;
  }

  .footer-accordion__content {
    display: none;
    width: 100%;
    height: auto;
    opacity: 1;
  }

  .footer-accordion__card.is-expanded .footer-accordion__content {
    display: block;
  }

  .footer-accordion__inner {
    padding: 0 1rem 1rem 1rem;
    max-width: none;
  }
}
</style>

<script>
  function initFooterAccordion() {
    const container = document.querySelector("[data-footer-accordion]");
    if (!container) return;

    const cards = container.querySelectorAll("[data-accordion-card]");

    cards.forEach((card) => {
      const toggle = card.querySelector("[data-toggle]");

      if (!toggle) return;

      toggle.addEventListener("click", () => {
        const isExpanded = card.classList.contains("is-expanded");

        // Close all cards
        cards.forEach((c) => {
          c.classList.remove("is-expanded");
          const t = c.querySelector("[data-toggle]");
          if (t) t.setAttribute("aria-expanded", "false");
        });

        // If this card wasn't expanded, expand it
        if (!isExpanded) {
          card.classList.add("is-expanded");
          toggle.setAttribute("aria-expanded", "true");
        }
      });
    });
  }

  document.addEventListener("DOMContentLoaded", initFooterAccordion);
  document.addEventListener("astro:page-load", initFooterAccordion);
</script>